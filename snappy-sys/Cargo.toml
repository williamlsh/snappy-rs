[package]
name = "snappy-sys"
version = "0.1.0"
edition = "2021"
links = "snappy"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"

[build-dependencies]
cmake = "0.1"
pkg-config = "0.3"
